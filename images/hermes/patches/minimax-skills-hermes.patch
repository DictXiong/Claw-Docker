diff --git a/skills/minimax-docx/SKILL.md b/skills/minimax-docx/SKILL.md
--- a/skills/minimax-docx/SKILL.md
+++ b/skills/minimax-docx/SKILL.md
@@ -38,18 +38,16 @@ description: >
 
 ## Setup
 
-**First time:** `bash scripts/setup.sh` (or `powershell scripts/setup.ps1` on Windows, `--minimal` to skip optional deps).
+**Container environment:** Dependencies and a precompiled CLI are installed in the image. The image does not include the .NET SDK: use the CLI for all operations, treat the C# sections as implementation reference only, and do not run `dotnet` or `scripts/setup.sh`.
 
-**First operation in session:** `scripts/env_check.sh` — do not proceed if `NOT READY`. (Skip on subsequent operations within the same session.)
+**First operation in session:** `bash /opt/minimax-skills/skills/minimax-docx/scripts/env_check.sh` — do not proceed if `NOT READY`. (Skip on subsequent operations within the same session.)
 
 ## Quick Start: Direct C# Path
 
-When the task requires structural document manipulation (custom styles, complex tables, multi-section layouts, headers/footers, TOC, images), write C# directly instead of wrestling with CLI limitations. Use this scaffold:
+The image uses the precompiled CLI for structural document manipulation. The following C# scaffold is reference material only; do not try to compile or execute it in the container:
 
 ```csharp
-// File: scripts/dotnet/task.csx  (or a new .cs in a Console project)
-// dotnet run --project scripts/dotnet/MiniMaxAIDocx.Cli -- run-script task.csx
-#r "nuget: DocumentFormat.OpenXml, 3.2.0"
+// Reference only. Use $CLI commands in this container.
 
 using DocumentFormat.OpenXml;
 using DocumentFormat.OpenXml.Packaging;
@@ -70,7 +68,7 @@ mainPart.Document = new Document(new Body());
 
 All CLI commands below use `$CLI` as shorthand for:
 ```bash
-dotnet run --project scripts/dotnet/MiniMaxAIDocx.Cli --
+/opt/minimax-docx-cli/MiniMaxAIDocx.Cli
 ```
 
 ## Pipeline routing
@@ -113,6 +111,6 @@ Read `references/scenario_a_create.md`, `references/typography_guide.md`, and `r
 **Choose your path:**
 - **Simple** (plain text, minimal formatting): use CLI — `$CLI create --type report --output out.docx --config content.json`
-- **Structural** (custom styles, multi-section, TOC, images, complex tables): write C# directly. Read the relevant `Samples/*.cs` first.
+- **Structural** (custom styles, multi-section, TOC, images, complex tables): use the closest supported CLI operation and read `Samples/*.cs` only as implementation reference. If the CLI is insufficient, use LibreOffice or Python OOXML tooling in a writable task directory; do not invoke `dotnet`.
 
 CLI options: `--type` (report|letter|memo|academic), `--title`, `--author`, `--page-size` (letter|a4|legal|a3), `--margins` (standard|narrow|wide), `--header`, `--footer`, `--page-numbers`, `--toc`, `--content-json`.
 
@@ -124,7 +122,7 @@ Read `references/scenario_b_edit_content.md` first. Preview → analyze → edit → v
 
 **Choose your path:**
 - **Simple** (text replacement, placeholder fill): use CLI subcommands.
-- **Structural** (add/reorganize sections, modify styles, manipulate tables, insert images): write C# directly. Read `references/openxml_element_order.md` and the relevant `Samples/*.cs`.
+- **Structural** (add/reorganize sections, modify styles, manipulate tables, insert images): use the closest supported CLI operation. If necessary, use LibreOffice or Python OOXML tooling in a writable task directory; do not invoke `dotnet`.
 
 Available CLI edit subcommands:
 - `replace-text --find "X" --replace "Y"`
@@ -151,6 +149,6 @@ $CLI apply-template --input source.docx --template template.docx --output out.do
 ```
 
-For complex template operations (multi-template merge, per-section headers/footers, style merging), write C# directly — see Critical Rules below for required patterns.
+For complex template operations (multi-template merge, per-section headers/footers, style merging), use the CLI where supported; otherwise use LibreOffice or Python OOXML tooling in a writable task directory. See Critical Rules below for required patterns, and do not invoke `dotnet`.
 
 Run the **validation pipeline**, then the **hard gate-check**:
 ```bash
diff --git a/skills/minimax-docx/references/xsd_validation_guide.md b/skills/minimax-docx/references/xsd_validation_guide.md
--- a/skills/minimax-docx/references/xsd_validation_guide.md
+++ b/skills/minimax-docx/references/xsd_validation_guide.md
@@ -3,14 +3,14 @@
 ## Running Validation
 
 ```bash
 # Validate against the WML subset schema
-dotnet run --project minimax-docx validate input.docx --xsd assets/xsd/wml-subset.xsd
+$CLI validate input.docx --xsd assets/xsd/wml-subset.xsd
 
 # Validate against business rules (REQUIRED for Scenario C gate-check)
-dotnet run --project minimax-docx validate input.docx --xsd assets/xsd/business-rules.xsd
+$CLI validate input.docx --xsd assets/xsd/business-rules.xsd
 
 # Validate against both
-dotnet run --project minimax-docx validate input.docx --xsd assets/xsd/wml-subset.xsd --xsd assets/xsd/business-rules.xsd
+$CLI validate input.docx --xsd assets/xsd/wml-subset.xsd --xsd assets/xsd/business-rules.xsd
 ```
 
 ---
diff --git a/skills/minimax-docx/scripts/env_check.sh b/skills/minimax-docx/scripts/env_check.sh
--- a/skills/minimax-docx/scripts/env_check.sh
+++ b/skills/minimax-docx/scripts/env_check.sh
@@ -7,5 +7,6 @@ SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 PROJECT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
 DOTNET_DIR="$SCRIPT_DIR/dotnet"
+SELF_CONTAINED_CLI="/opt/minimax-docx-cli/MiniMaxAIDocx.Cli"
 
 # Force English output for dotnet CLI
 export DOTNET_CLI_UI_LANGUAGE=en
@@ -29,7 +30,9 @@ case "$(uname -s)" in
 esac
 
 # --- Critical: .NET SDK ---
-if ! command -v dotnet &>/dev/null; then
+if [ -x "$SELF_CONTAINED_CLI" ]; then
+    printf "[OK]      %-14s precompiled CLI\n" "runtime"
+elif ! command -v dotnet &>/dev/null; then
     printf "[FAIL]    %-14s not found\n" "dotnet"
     echo ""
     echo "  .NET SDK is REQUIRED. Install it:"
@@ -68,7 +71,9 @@ else
 
 # --- Critical: NuGet packages ---
 if [ -d "$DOTNET_DIR" ]; then
-    if [ -f "$DOTNET_DIR/MiniMaxAIDocx.Cli/bin/Debug/net10.0/MiniMaxAIDocx.Cli.dll" ] || \
+    if [ -x "$SELF_CONTAINED_CLI" ]; then
+        printf "[OK]      %-14s precompiled\n" "project"
+    elif [ -f "$DOTNET_DIR/MiniMaxAIDocx.Cli/bin/Debug/net10.0/MiniMaxAIDocx.Cli.dll" ] || \
        [ -f "$DOTNET_DIR/MiniMaxAIDocx.Cli/bin/Debug/net8.0/MiniMaxAIDocx.Cli.dll" ]; then
         printf "[OK]      %-14s built\n" "project"
     else
diff --git a/skills/minimax-pdf/SKILL.md b/skills/minimax-pdf/SKILL.md
--- a/skills/minimax-pdf/SKILL.md
+++ b/skills/minimax-pdf/SKILL.md
@@ -177,9 +177,10 @@ bash scripts/make.sh reformat \
 
 ## Environment
 
+**Hermes container:** Dependencies are managed by the image. Run `check`, but do not run `fix` or install Playwright at runtime. `render_cover.js` uses Hermes' bundled Chromium directly.
+
 ```bash
 bash scripts/make.sh check   # verify all deps
-bash scripts/make.sh fix     # auto-install missing deps
 bash scripts/make.sh demo    # build a sample PDF
 ```
 
@@ -189,4 +191,4 @@ bash scripts/make.sh demo    # build a sample PDF
 | `reportlab` | `render_body.py` | `pip install reportlab` |
 | `pypdf` | fill, merge, reformat | `pip install pypdf` |
 | Node.js 18+ | `render_cover.js` | system |
-| `playwright` + Chromium | `render_cover.js` | `npm install -g playwright && npx playwright install chromium` |
+| Chromium Headless Shell | `render_cover.js` | managed by the Hermes image |
diff --git a/skills/minimax-pdf/scripts/make.sh b/skills/minimax-pdf/scripts/make.sh
--- a/skills/minimax-pdf/scripts/make.sh
+++ b/skills/minimax-pdf/scripts/make.sh
@@ -74,12 +74,18 @@ cmd_check() {
     ok=false
   fi
 
-  # Playwright
-  if node -e "require('playwright')" 2>/dev/null || \
-     node -e "require(require('child_process').execSync('npm root -g').toString().trim()+'/playwright')" 2>/dev/null; then
-    green "  ✓ playwright"
+  # Hermes-bundled Chromium
+  local chromium_bin="${AGENT_BROWSER_EXECUTABLE_PATH:-}"
+  local browser_root="${PLAYWRIGHT_BROWSERS_PATH:-/opt/hermes/.playwright}"
+  if [[ ! -x "$chromium_bin" && -d "$browser_root" ]]; then
+    chromium_bin="$(find "$browser_root" -type f -executable \
+      \( -name chrome -o -name chromium -o -name chrome-headless-shell \
+         -o -name headless_shell -o -name chromium-browser \) -print -quit 2>/dev/null)"
+  fi
+  if [[ -x "$chromium_bin" ]]; then
+    green "  ✓ Hermes Chromium"
   else
-    yellow "  ⚠ playwright not found  (run: make.sh fix)"
+    yellow "  ⚠ Hermes Chromium not found — rebuild the container image"
     ok=false
   fi

@@ -92,7 +98,7 @@ cmd_check() {
   if python3 -c "import matplotlib" 2>/dev/null; then
     green "  ✓ matplotlib (math, chart, flowchart blocks enabled)"
   else
-    yellow "  ⚠ matplotlib not installed — math/chart/flowchart blocks degrade to text  (run: make.sh fix)"
+    yellow "  ⚠ matplotlib not installed — optional math/chart/flowchart blocks degrade to text"
   fi

   if $ok; then
@@ -101,32 +107,9 @@ cmd_check() {
 
 # ── fix ────────────────────────────────────────────────────────────────────────
 cmd_fix() {
-  bold "Installing missing dependencies..."
-  local rc=0
-
-  # Python packages
-  if command -v python3 &>/dev/null; then
-    python3 -m pip install --break-system-packages -q reportlab pypdf matplotlib 2>/dev/null \
-      || python3 -m pip install -q reportlab pypdf matplotlib 2>/dev/null \
-      || { yellow "  pip install failed — try: pip install reportlab pypdf matplotlib"; rc=3; }
-    green "  ✓ Python packages installed (reportlab, pypdf, matplotlib)"
-  fi
-
-  # Playwright
-  if command -v npm &>/dev/null; then
-    npm install -g playwright --silent 2>/dev/null && \
-    npx playwright install chromium --silent 2>/dev/null && \
-    green "  ✓ Playwright + Chromium installed" || \
-    { yellow "  playwright install failed — try manually"; rc=3; }
-  else
-    yellow "  npm not found — cannot install Playwright automatically"
-    rc=2
-  fi
-
-  if [[ $rc -eq 0 ]]; then
-    green "\nAll dependencies installed. Run: bash make.sh check"
-  fi
-  exit $rc
+  yellow "Dependencies are managed by the immutable Hermes image."
+  yellow "Rebuild the image if the following check reports a missing dependency."
+  cmd_check
 }
 
 # ── run ────────────────────────────────────────────────────────────────────────
