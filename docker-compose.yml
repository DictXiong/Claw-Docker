services:
  openclaw-jarvis:
    build:
      context: .
      args:
        MINIMAX_SKILLS_REF: "${MINIMAX_SKILLS_REF:?MINIMAX_SKILLS_REF must be set in .env}"
    image: "${OPENCLAW_IMAGE:-openclaw-jarvis:local}"
    container_name: openclaw-jarvis
    restart: unless-stopped
    stop_grace_period: 30s
    security_opt:
      - no-new-privileges:true
    shm_size: 1gb
    volumes:
      - type: bind
        source: /data0/opt/claw/jarvis/.openclaw
        target: /home/node/.openclaw
        bind:
          create_host_path: false
      - type: bind
        source: /tank1/entity.backup1/Obsidian
        target: /home/node/.openclaw/workspace/obsidian
        bind:
          create_host_path: false
      - type: bind
        source: /tank1/entity.backup1/OneDrive
        target: /home/node/.openclaw/workspace/onedrive-readonly
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: /etc/localtime
        target: /etc/localtime
        read_only: true
        bind:
          create_host_path: false
    dns:
      - "10.91.0.69"
      - "1.0.0.1"
    ports:
      - "${OPENCLAW_GATEWAY_PORT:-18789}:18789"
    environment:
      TZ: "${TZ:-Asia/Shanghai}"
      HOME: /home/node
      OPENCLAW_HOME: /home/node/.openclaw
      OPENCLAW_STATE_DIR: /home/node/.openclaw
      OPENCLAW_CONFIG_PATH: /home/node/.openclaw/openclaw.json
      OPENCLAW_WORKSPACE_DIR: /home/node/.openclaw/workspace
      OPENCLAW_DISABLE_BONJOUR: "1"
      OPENCLAW_SERVICE_REPAIR_POLICY: external
      XDG_CACHE_HOME: /home/node/.cache
